import styled from 'styled-components';

export const ETSLogo = styled.a`
  &&& {
    min-height: 64px;
    background: #7fc650;
    color: white;
    text-align: center;
    font-family: 'Roboto', sans-serif;
    font-weight: 700;
    letter-spacing: 1px;
    cursor: default;

    &:hover {
      background: #7fc650;
      color: white;
    }
  }
`;
