<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="version" content={{htmlWebpackPlugin.options.version}} />
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="-1">
    <link rel="manifest" href="{{htmlWebpackPlugin.options.publicPath}}manifest.json">
    <meta http-equiv="Cache-Control" content="max-age=86400" />
    <meta name="mobile-web-app-capable" content="yes">

    <meta property="og:site_name" content="Ets">
    <meta property="og:title" content="">
    <meta property="og:type" content="object">
    <meta property="og:description" content="Единая транспортная система">
    <meta property="og:image" content="{{htmlWebpackPlugin.options.publicPath}}icons/icon-192x192.png">

    <meta name="apple-mobile-web-app-status-bar-style" content="{{htmlWebpackPlugin.options.themeColor}}">
    <meta name="theme-color" content="{{htmlWebpackPlugin.options.themeColor}}" />
    <title>{{htmlWebpackPlugin.options.title}}</title>
    
    <script>
      if ('serviceWorker' in navigator) {
        if (!Boolean({{htmlWebpackPlugin.options.DEVELOPMENT}}) && false) {
          window.addEventListener('load', function() {
            navigator.serviceWorker.register('sw.js', { scope: '{{htmlWebpackPlugin.options.publicPath}}' || '.' }).then(function(registration) {
              // Registration was successful
              registration.update();
              console.log('ServiceWorker registration successful with scope: ', registration.scope);
            }, function(err) {
              // registration failed :(
              console.log('ServiceWorker registration failed: ', err);
            }).catch(function(err) {
              console.log(err)
            });
          });
        }
      } else {
        // console.log('service worker is not supported');
      }
    </script>
    <style>
      noscript {
        position: absolute;
        top: 0;
        height: 100%;
        width: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
        z-index: 100000;
        background: white;
      }
      html, body {
        height: 100%;
        margin: 0;
        font-family: "Helvetica Neue",Helvetica,Arial,sans-serif;
      }
      #image_container {
        margin: 0;
        font-family: "Helvetica Neue",Helvetica,Arial,sans-serif;
      }

      ::selection {
        background: #7fc650; /* WebKit/Blink Browsers */
        color: white;
      }
      ::-moz-selection {
        background: #7fc650; /* Gecko Browsers */
        color: white;
      }
      #main-loading {
        position: absolute;
        top: 0;
        width: 100%;
        height: 100%;
        background-image: url(./images/bg.jpg);
        display: flex;
        justify-content: center;
        align-items: center;
        background-size: cover;
        background-position: center;
        position: absolute;
        opacity: 1;
        will-change: opacity;
        transition: opacity 0.3s;
        z-index: 10000;
      }
      #main-loading>div {
        background-color: grey;
        color: white;
        padding: 20px;
        font-size: 40px;
      }
    </style>
  </head>
  <body>
    <div id="container"></div>
    <div id="modal_container"></div>
    <div id="image_container"></div>
    <div id="notification_container"></div>
    <div id="main-loading">
      <div>Идет загрузка приложения...</div>
    </div>
    <noscript><p>Ваш браузер не поддерживает javascript</p></noscript> 
  </body>
</html>
